// Cache tag for partner portal data. Kept in its own file so collection hooks can
// import it without pulling in the Payload config (which would import them back).
export const PARTNERS_CACHE_TAG = 'partners'
